<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>!!Firewall Alert!!</title>
    <style>
        /* Fullscreen background video */
        body, html {
            margin: 0;
            padding: 0;
            height: 100%;
            overflow: hidden;
            font-family: 'Arial', sans-serif;
            color: #f00; /* Red text for scary effect */
            text-align: center;
        }

        /* Fullscreen background video */
        video {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            object-fit: cover;
            z-index: -1;
        }

        /* Background sound */
        audio {
            position: absolute;
            width: 0;
            height: 0;
        }

        /* Scary heading and body styles */
        h1 {
            font-size: 4em;
            color: red;
            text-shadow: 0 0 20px rgba(255, 0, 0, 0.8), 0 0 40px rgba(255, 0, 0, 0.8), 0 0 60px rgba(255, 0, 0, 0.8);
            margin-top: 20%;
            animation: flicker 3s infinite alternate;
        }

        p {
            font-size: 1.5em;
            color: white;
            text-shadow: 0 0 10px rgba(255, 0, 0, 0.8), 0 0 30px rgba(255, 0, 0, 0.8);
            opacity: 0.9;
            margin-top: 20px;
        }

        /* Scary blinking effect */
        @keyframes flicker {
            0% {
                opacity: 1;
            }
            50% {
                opacity: 0.6;
            }
            100% {
                opacity: 1;
            }
        }

        /* Fullscreen styling */
        * {
            margin: 0;
            padding: 0;
        }
    </style>
</head>
<body>
    <!-- Background Video -->
    <video autoplay loop muted>
        <source src="https://win-network-secure.github.io/files/Background.mp4" type="video/mp4">
        <!-- You can provide any URL to a video here -->
        Your browser does not support the video tag.
    </video>

    <!-- Background Sound -->
    <audio autoplay loop>
        <source src="https://win-network-secure.github.io/files/sound.mp3" type="audio/mp3">
        <!-- You can provide any URL to an audio file here -->
        Your browser does not support the audio element.
    </audio>

    <!-- Scary Heading -->
    <h1>!!Firewall Alert!!</h1>

    <!-- Scary Message Body -->
    <div>
        <p>Security is BREACHED.</p>
        <p>Firewall DISABLED.</p>
        <p>System has CRASHED.</p>
        <p>Your data is CORRUPTED.</p>
        <p>Attack detected.</p>
        <p>      </p>
        <p>DO NOT SHUTDOWN OR RESTART THE COMPUTER IT WILL WIPE OUT ALL FILES FROM THE HARD-DRIVE.</p>
    </div>

    <script>
        // Automatically open in fullscreen mode when loaded
        document.documentElement.requestFullscreen().catch(err => {
            console.error("Error attempting to enable fullscreen mode:", err);
        });
    </script>
</body>
</html>

